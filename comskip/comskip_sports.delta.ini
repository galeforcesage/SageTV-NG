; ======================================
; comskip_sports.delta.ini (ID=1)
; Conservative bias for sports
; Applied as base(0) + sports(1)
; ======================================

; Reduce over-detection — black + logo only
detect_method=43
punish_no_logo=0

; Gameplay segments are long — raise the floor
min_show_segment_length=600
min_commercialbreak=60

; Reduce chatter
verbose=5
